{
    "name": "eclipse-s-core",
    "image": "ghcr.io/eclipse-score/devcontainer:1.0.0",
    "containerUser": "root",
    "initializeCommand": "mkdir -p ${localEnv:HOME}/.cache/bazel",
    "runArgs": [
        "--privileged"
    ],
    "containerEnv": {
        "HTTP_PROXY": "http://172.17.0.1:3129",
        "HTTPS_PROXY": "http://172.17.0.1:3129",
        "http_proxy": "http://172.17.0.1:3129",
        "https_proxy": "http://172.17.0.1:3129",
        "NO_PROXY": "127.0.0.1,localhost,::1,.bosch.com"
    },
    // "dockerDaemonFile": "/var/run/docker.sock",
    "mounts": [
        "source=/home/mdl2kor/newDesign/kuksa/grpc,target=/workspaces/grpc,type=bind",
        "source=/home/mdl2kor/newDesign/kuksa/score_communication,target=/workspaces/score_communication,type=bind",
        "source=/home/mdl2kor/newDesign/kuksa/vsomeip,target=/workspaces/vsomeip,type=bind",
        "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind,consistency=cached"
    ],
    "onCreateCommand": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends python3 openssl curl ca-certificates python3-venv && python3 -m pip install --break-system-packages --no-cache-dir requests && python3 .devcontainer/install_certs.py && sudo mkdir -p /usr/local/share/ca-certificates/bosch && sudo cp certs/* /usr/local/share/ca-certificates/bosch && sudo update-ca-certificates && echo \"startup --host_jvm_args=-Djavax.net.ssl.trustStore=/etc/ssl/certs/java/cacerts --host_jvm_args=-Djavax.net.ssl.trustStorePassword=changeit\" | sudo tee -a /etc/bazel.bazelrc && curl -fsSL https://sh.rustup.rs | sh -s -- -y && export PATH=$HOME/.cargo/bin:$PATH && rustup target add aarch64-unknown-linux-gnu && (sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu binutils-aarch64-linux-gnu libc6-arm64-cross libc6-dev-arm64-cross linux-libc-dev-arm64-cross || echo 'WARNING: aarch64 cross-compilation packages not available, skipping')",
    "customizations": {
        "vscode": {
            "extensions": [
                "detachhead.basedpyright",
                "akiramiyakoda.cppincludeguard",
                "github.vscode-github-actions",
                "streetsidesoftware.code-spell-checker"
            ]
        }
    }
}
